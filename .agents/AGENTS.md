# Project Rules

## Git Workflow
- NEVER automatically run `git push` after committing or editing files. ALWAYS wait for an explicit user instruction/command before pushing to remote repositories (GitHub).
